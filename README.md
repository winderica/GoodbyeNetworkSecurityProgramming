# GoodbyeNetworkSecurityProgramming

```
Server <- Node-addon-api -> Electron <- IPC -> React
 C++                                             ↑
Client <-------------- Node-addon-api ------------
```

\* *C++ addon is built using CMake instead of node-gyp*